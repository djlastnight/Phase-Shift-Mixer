<UserControl x:Class="PsMixer.LaneButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Image DataContext="{Binding RelativeSource={RelativeSource AncestorType=UserControl}}"
           MouseLeftButtonDown="Image_MouseLeftButtonDown">
        <Image.Style>
            <Style TargetType="Image">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsChecked}" Value="True">
                        <Setter Property="Source" Value="{Binding CheckedSource}"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsChecked}" Value="False">
                        <Setter Property="Source" Value="{Binding UncheckedSource}"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Image.Style>
    </Image>
</UserControl>